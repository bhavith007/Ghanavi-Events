# Ghanavi Events — React (Vite) Starter

This is a minimal starter scaffold following the project specification in `context.md`.

Quick start (PowerShell):

```powershell
cd C:\Users\DELL\Downloads\Ghanavi
npm install
npm run dev
```

Notes:
- Uses Tailwind CSS utilities for all styling.
- Entrance animations use `framer-motion`.
- Icons via `lucide-react`.
- Tailwind config includes `gold`, `gold-dim`, `dark`, and `glass` per spec.
